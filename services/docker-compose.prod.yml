version: "3.9"

services:
  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
      target: production
    env_file:
      - ./auth-service/.env.prod
    # No volumes in prod
    depends_on: []

